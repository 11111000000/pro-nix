use flake

# Ensure direnv loads the flake devShell when entering the repo
if [ -f flake.nix ]; then
  eval "$(nix print-dev-env --json --extra-experimental-features nix-command | jq -r '.[] | ""')" || true
fi
