;;; ui.el --- compatibility shim -> pro-ui -*- lexical-binding: t; -*-
;; Legacy shim that maps `ui` to `pro-ui` and provides legacy feature `ui`.

(require 'pro-ui nil t)
(provide 'ui)

;;; ui.el ends here
