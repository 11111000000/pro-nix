;;; chat.el --- compatibility shim -> pro-chat -*- lexical-binding: t; -*-
;; Legacy shim mapping `chat` to `pro-chat` and providing legacy feature `chat`.

(require 'pro-chat nil t)
(provide 'chat)

;;; chat.el ends here
