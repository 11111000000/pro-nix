;;; packages.el --- compatibility shim -> pro-packages -*- lexical-binding: t; -*-
;; Legacy shim mapping `packages` to `pro-packages` and providing old feature.

(require 'pro-packages nil t)
(provide 'packages)

;;; packages.el ends here
