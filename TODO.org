* pi-crew statusbar flicker
- Проблема: при активных subagents статусбар постоянно анимирует спиннер, что вызывает мерцание.
- Предложенное решение:
  - Увеличить интервал смены фрейма спиннера (SPINNER_INTERVAL_MS = 400 ms).
  - Анимировать спиннер только когда сессия простаивает (ctx.isIdle() === true). При активной сессии показывать статичный значок (например ⏳).
  - Опционально: использовать статичный индикатор вместо спиннера или полностью убрать анимацию.
- Файл: extension/ui.ts (pi-crew)
- Патч (кратко):
  - const SPINNER_INTERVAL_MS = 400
  - syncWidgetText(state, agents, animate = false) { ... }
  - setInterval callback: вычислять animate = ctx.isIdle(), увеличивать frameIndex только если animate === true
- Тест: перезапустить pi-сессию, запустить subagents, проверить поведение статусбара в активной/неактивной сессии.
- Приоритет: low
- Ответственный: @melihmucuk или локальный разработчик

# end of TODO
